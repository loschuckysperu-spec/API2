# Proyecto corregido

Correcciones:
- Agregado `cors` en dependencies.
- Node ajustado a `22.x` para Vercel.
- API en `api/datos.js`.
- Panel en `public/index.html`.
- Rutas `/datos` y `/api/datos` listas.

Para subir:

```bash
npm install
git add .
git commit -m "corregir api y cors"
git push
```
